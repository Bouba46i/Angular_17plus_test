import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

@NgModule({
	declarations: [],
	imports: [CommonModule],
})
export class SharedModule {}

// generer une pipe : ng generate pipe shared/pipes/pipe-name
// generer une directives : ng generate directive shared/directives/directive-name
